class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
       return nums*2
       #n=len(nums)
       #ans=[0]*(2*n)
       #i=0
       #while i < 2*n:
        #ans[i] = nums[i%n]
        #i+=1
       #return ans 
